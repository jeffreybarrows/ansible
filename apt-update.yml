---
- name: Update apt cache on Ubuntu systems
  hosts: all
  become: true
  become_user: root
  become_method: sudo

  tasks:
    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
