---
- name: Update Packages
  hosts: 192.168.1.40  # Replace with your target hosts or inventory group
  become: yes
  become_user: root
  become_method: sudo
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
